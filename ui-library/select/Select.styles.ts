import { themeStyles } from "ui-library/__theme/Theme.styles"

export const selectStyles = {
  label: `block mb-2 text-sm font-medium ${themeStyles.textColor.standard[600]}`,
  select: `${themeStyles.backgroundColor.standard[50]} ${themeStyles.textColor.standard[600]} text-sm rounded-lg block w-full p-3 ${themeStyles.border.standard}`,
}
