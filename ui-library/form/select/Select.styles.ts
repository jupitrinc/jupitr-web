import { themeStyles } from "ui-library/_theme/Theme.styles"

export const selectStyles = {
  select: `${themeStyles.backgroundColor.standard[50]} ${themeStyles.textColor.standard[600]} text-sm block w-full p-2.5 ${themeStyles.border.standard} ${themeStyles.disabled}`,
}
