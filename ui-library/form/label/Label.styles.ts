import { themeStyles } from "ui-library/_theme/Theme.styles"

export const labelStyles = {
  label: `w-full text-sm `,
  label_valid: `${themeStyles.textColor.standard[500]}`,
  label_invalid: `${themeStyles.textColor.dangerous[400]}`,
}
