import { themeStyles } from "ui-library/_theme/Theme.styles"

export const dividerStyles = {
  ligth: `h-[0.5px] ${themeStyles.backgroundColor.standard[200]} border-0 w-full`,
  dark: `h-[0.5px] ${themeStyles.backgroundColor.standard[300]} border-0 w-full`,
}
