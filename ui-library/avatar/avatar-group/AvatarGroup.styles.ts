import { themeStyles } from "ui-library/__theme/Theme.styles"

export const avatarGroupStyles = {
  container: "flex -space-x-2 overflow-hidden",
  label: `${themeStyles.textColor.standard[600]} self-center pl-3 text-xs`,
}
