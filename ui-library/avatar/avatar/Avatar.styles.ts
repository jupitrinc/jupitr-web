export const avatarStyles = {
  container:
    "inline-flex overflow-hidden justify-center items-center bg-gray-100 rounded-full text-gray-600 border-2 border-white",
  icon: "font-medium text-gray-300",
  image: "object-cover",
  size: (size: number) => `w-${size} h-${size}`,
}
